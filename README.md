# electron-sqlite3-quick-start

An example of electron app with sqlite3

Install dependencies

* `$ npm i`

Make running

* `$ npm run postinstall`
* `$ npm run start`

Make packaging
* `$ npm run package`
