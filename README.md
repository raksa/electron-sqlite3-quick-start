# electron-sqlite3-quick-start

An example of electron app with sqlite3

Make running
````
>cd electron-sqlite3-quick-start

>npm install

>npm run rebuild

>npm run start
````

Make packaging
````
>cd electron-sqlite3-quick-start

>npm install

>npm run package
````